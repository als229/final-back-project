<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kh.finalproject.content.model.mapper.FestivalMapper">

  <insert id="insertFestival" parameterType="com.kh.finalproject.content.model.vo.Festival">
    INSERT INTO TB_FESTIVAL (
      CONTENT_ID,
      PROGRAM,
      EVENT_EXP,
      SPONSOR,
      USETIME_FESTIVAL,
      EVENT_START_DATE,
      EVENT_END_DATE
    ) VALUES (
      #{contentId},
      #{program},
      #{eventExp},
      #{sponsor},
      #{usetimeFestival},
      #{eventStartDate},
      #{eventEndDate}
    )
  </insert>

</mapper>